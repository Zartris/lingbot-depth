*.pyc
*.egg-info
*.egg
__pycache__
.idea
.vscode
workspace*/
result*/
ckpt/

# distillation harness outputs
distill/.cache/
distill/runs/*
# keep the aggregate result log + the teacher baseline, ignore everything else
# (per-run checkpoints/snapshots + regenerable telemetry like the sweeps/comparison)
!distill/runs/results.csv
!distill/runs/teacher_baseline.json